"use strict";

// This file is intentionally tiny and contains no environment credentials.
// It is loaded before app.js so public-only guards are active from first paint.
globalThis.KW_BENCH_PUBLIC_MODE = true;
